import BusinessSuite from '@/components/studio/BusinessSuite';
"use client"; import { useState } from "react"; import { Card, SectionLabel, Button } from "@/components/studio/ui"; import { DEFAULT_RIDER, TechRider } from "@/lib/studio/business";  export default function PlannerPage() {   const [rider, setRider] = useState<TechRider>(DEFAULT_RIDER);   const [items, setItems] = useState(DEFAULT_RIDER.equipment);    const downloadRider = () => {     const text = "TECHNICAL RIDER: " + rider.artistName + "\n\nEQUIPMENT:\n" + items.map(i => "- " + i).join("\n") + "\n\nMONITORING:\n" + rider.monitoring;     const blob = new Blob([text], { type: "text/plain" });     const url = URL.createObjectURL(blob);     const a = document.createElement("a"); a.href = url; a.download = "Tech_Rider_Emy.txt"; a.click();   };    return (     <div className="space-y-5">       <h1 className="brand-text-grad text-3xl font-extrabold tracking-tight">?? Gig Planner</h1>       <div className="grid gap-4 md:grid-cols-2">         <Card className="p-5">           <SectionLabel>Technical Rider</SectionLabel>           <div className="mt-4 space-y-3">             {items.map((item, i) => (               <div key={i} className="flex gap-2">                 <input value={item} onChange={e => { const n = [...items]; n[i] = e.target.value; setItems(n); }} className="flex-1 bg-zinc-950 border border-zinc-800 rounded-lg p-2 text-xs" />               </div>             ))}             <Button variant="ghost" onClick={() => setItems([...items, "New Item"])}>+ Add Item</Button>           </div>           <Button className="mt-4 w-full" onClick={downloadRider}>Download TXT Rider</Button>         </Card>         <Card className="p-5">           <SectionLabel>Stage Plot</SectionLabel>           <div className="mt-4 aspect-video bg-zinc-950 rounded-2xl border-2 border-dashed border-zinc-800 flex items-center justify-center text-zinc-600 text-xs">             Visual Stage Plot Drag-and-Drop coming soon           </div>         </Card>       </div>     </div>   ); }
